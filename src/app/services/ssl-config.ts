/**
 * Este archivo contiene utilidades para configurar SSL para desarrollo.
 * Nota: Se ha eliminado la lógica SSR para simplificar la aplicación.
 * Puede restaurarse en el futuro cuando se necesite SSR.
 */

// Este archivo se mantiene como referencia para futuras implementaciones de SSL
// pero no se usa activamente en la aplicación simplificada
